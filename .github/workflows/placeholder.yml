name: CI Placeholder

on:
  workflow_dispatch:

jobs:
  placeholder-job:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Idle Step
        run: echo "CI is currently in placeholder mode. No actions performed."